on damage:

    set {_name} to coreGetEntity(name of attacker)
    
    if {_name} is "Zombie":

        set damage to 0

        set {_damage} to 1
        set {_final} to coreDamagePlayer(victim, {_damage})

        add {_final} to damage