on death:

    set {_name} to coreGetEntity(name of victim)

    if {_name} is "Odin":

        set {_xp} to 1040
        add 1 to {boss::odin::%attacker's uuid%}
        coreAddXp(attacker, {_xp})
        coreCheckLevel(attacker)