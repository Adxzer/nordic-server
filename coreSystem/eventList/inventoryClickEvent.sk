on inventory click:

    if event-inventory is player's inventory:

        if index of event-slot is 8 or 7:
            cancel event
    else:
        play sound "UI_BUTTON_CLICK" with volume 0.2 to player